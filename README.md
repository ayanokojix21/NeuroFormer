# NeuroFormer
NeuroFormer is a modular Transformer architecture built from scratch in PyTorch that supports:  Decoder-only (like GPT) — for language modeling and chatbots  Encoder-only (like BERT) — for tasks like question answering  Encoder-Decoder (like T5/BART) — for machine translation
